#crear una variable con el número 8, una con el número 10.5, y una con la palabra “ejercicio”.
numeroEntero = 8
numeroDecimal = 10.5
palabra = "ejercicio"

#crear un set que contendrá cada una de las variables que creamos, y será asignado a una variable.
setVaribles = {numeroEntero, numeroDecimal, palabra}

#crearemos una lista que contendrá el set creado anteriormente, y una variable con
#el valor lógico False. Esta lista la asignaremos a una variable que luego imprimiremos en pantalla.
booleano = False
lista = [setVaribles, booleano]

print(f'La lista creada es {lista}')
